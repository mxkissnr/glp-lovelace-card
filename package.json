{
  "name": "glp-lovelace-card",
  "private": true,
  "scripts": {
    "test": "node --test",
    "test:coverage": "c8 --reporter=text --reporter=json-summary --check-coverage --lines=59 node --test",
    "build": "node --check glp-card.js",
    "lint": "eslint .",
    "screenshot": "node scripts/screenshot.mjs",
    "sync-to-integration": "bash scripts/sync-to-integration.sh"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "c8": "^12.0.0",
    "eslint": "^10.8.1",
    "globals": "^17.11.0",
    "playwright": "^1.62.1"
  }
}
